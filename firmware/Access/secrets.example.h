#pragma once

// Copie este arquivo para "secrets.h" (mesma pasta) e preencha com os valores
// reais. O "secrets.h" está listado no .gitignore e nunca deve ser
// versionado.

#define WIFI_SSID   "nome-da-rede"
#define WIFI_PASS   "senha-da-rede"

// URI completa do broker MQTT sobre WebSocket Secure, no formato wss://host/caminho
#define MQTT_URI    "wss://host/caminho"
#define MQTT_USER   "usuario-do-broker"
#define MQTT_PASS   "senha-do-broker"
